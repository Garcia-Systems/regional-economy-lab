# Data

YAML inputs use the authoritative [scenario schema](../docs/scenario-schema.md); authoring and package copies remain byte-identical.

Version 0.1.0 contains no empirical dataset. All scenario values are fictional or assumed
educational values, as labeled in YAML and documented in `docs/data-sources.md`.

